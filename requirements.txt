fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart
python-dotenv
numpy
pydantic
google-generativeai

# HEAVY LIBRARIES REMOVED FOR VERCEL STABILITY
# The code handles their absence via Lite Mode mocks.
# - librosa (Too large/heavy for startup)
# - scipy
# - matplotlib
# - chromadb (Too large)
# - tensorflow (Way too large)
